.DS_Store
.claude/settings.local.json

# Память проекта целиком вне git: репозиторий публичный, а в memory/ лежат
# откровенные внутренние заметки (риски, недоделки). Память живёт только локально.
memory/

images/
sila
.figma-token
*.token
*-token
.godaddy-token
.fal-token
.stripe*
.env
.env.*
hf_*.png
ring2-3_black_stone.png
